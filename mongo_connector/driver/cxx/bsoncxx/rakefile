
PACKAGE = 'recorto/mongo_connector/driver/bsoncxx'

INCLUDE = [
    "$(CORTO_INCLUDE)",
    "$(CORTO_INCLUDE)/bsoncxx"
]

LANGUAGE = 'c++'

NOCORTO = true

USE_PACKAGE=["/recorto/mongo_connector/driver/bson"]

require "#{ENV['CORTO_BUILD']}/package"
